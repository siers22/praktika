# Техническое задание

## Информационная система «Электронный каталог: Инвентаризация оборудования»

---

## 1. Общие сведения

### 1.1. Наименование системы

Электронный каталог «Инвентаризация оборудования» (далее — Система).

### 1.2. Назначение системы

Система предназначена для автоматизации процессов учёта, инвентаризации и контроля состояния оборудования организации. Система обеспечивает централизованное хранение данных об оборудовании, отслеживание его перемещений, списаний и текущего состояния.

### 1.3. Цели создания

- Обеспечить единое информационное пространство для учёта оборудования.
- Сократить трудозатраты на проведение инвентаризации.
- Повысить точность и актуальность данных об оборудовании.
- Обеспечить контроль движения оборудования между подразделениями.
- Предоставить инструменты формирования отчётности.

---

## 2. Стек технологий

| Компонент        | Технология                          |
| ---------------- | ----------------------------------- |
| Frontend         | Next.js 14+, TypeScript, Tailwind CSS |
| Backend (API)    | Go (Golang) 1.22+                   |
| База данных      | PostgreSQL 16+                      |
| Контейнеризация  | Podman, Podman Compose              |
| API-протокол     | REST (JSON)                         |
| Аутентификация   | JWT (Access + Refresh токены)       |
| Документация API | Swagger / OpenAPI 3.0               |

---

## 3. Роли пользователей

### 3.1. Администратор

- Полный доступ ко всем функциям системы.
- Управление пользователями и ролями.
- Настройка справочников (категории, подразделения, статусы).
- Просмотр журнала действий.

### 3.2. Ответственное лицо (Инвентаризатор)

- Добавление, редактирование и просмотр карточек оборудования.
- Проведение инвентаризации.
- Перемещение оборудования между подразделениями.
- Формирование отчётов.

### 3.3. Наблюдатель (Просмотр)

- Просмотр каталога оборудования (только чтение).
- Просмотр отчётов.

---

## 4. Функциональные требования

### 4.1. Модуль аутентификации и авторизации

| ID     | Требование                                                         |
| ------ | ------------------------------------------------------------------ |
| AUTH-1 | Регистрация пользователей (только администратором)                  |
| AUTH-2 | Авторизация по логину и паролю                                     |
| AUTH-3 | JWT-аутентификация (access-токен — 15 мин, refresh-токен — 7 дней) |
| AUTH-4 | Разграничение прав доступа на основе ролей (RBAC)                  |
| AUTH-5 | Выход из системы (инвалидация refresh-токена)                      |
| AUTH-6 | Смена пароля пользователем                                         |

### 4.2. Модуль управления оборудованием (CRUD)

| ID    | Требование                                                                   |
| ----- | ---------------------------------------------------------------------------- |
| EQ-1  | Создание карточки оборудования                                               |
| EQ-2  | Редактирование карточки оборудования                                         |
| EQ-3  | Удаление (мягкое — перевод в архив) карточки оборудования                    |
| EQ-4  | Просмотр карточки оборудования с полной информацией                          |
| EQ-5  | Список оборудования с пагинацией, сортировкой и фильтрацией                 |
| EQ-6  | Поиск оборудования по названию, инвентарному номеру, серийному номеру        |
| EQ-7  | Фильтрация по категории, статусу, подразделению, дате постановки на учёт     |
| EQ-8  | Присвоение уникального инвентарного номера (автогенерация или ручной ввод)    |
| EQ-9  | Загрузка фотографий оборудования (до 5 файлов, макс. 5 МБ каждый)           |
| EQ-10 | Экспорт списка оборудования в CSV                                            |

#### Карточка оборудования (поля)

| Поле                      | Тип          | Обязательное | Описание                                  |
| ------------------------- | ------------ | ------------ | ----------------------------------------- |
| id                        | UUID         | Да           | Уникальный идентификатор                  |
| inventory_number          | string       | Да           | Инвентарный номер (уникальный)            |
| name                      | string       | Да           | Наименование оборудования                 |
| description               | text         | Нет          | Описание / характеристики                 |
| category_id               | UUID (FK)    | Да           | Категория оборудования                    |
| serial_number             | string       | Нет          | Серийный / заводской номер                |
| model                     | string       | Нет          | Модель                                    |
| manufacturer              | string       | Нет          | Производитель                             |
| purchase_date             | date         | Нет          | Дата приобретения                         |
| purchase_price            | decimal      | Нет          | Стоимость приобретения                    |
| warranty_expiry           | date         | Нет          | Дата окончания гарантии                   |
| status                    | enum         | Да           | Статус (см. ниже)                         |
| department_id             | UUID (FK)    | Да           | Подразделение / местоположение            |
| responsible_person_id     | UUID (FK)    | Нет          | Ответственное лицо                        |
| photos                    | string[]     | Нет          | Пути к загруженным фотографиям            |
| notes                     | text         | Нет          | Примечания                                |
| created_at                | timestamp    | Да           | Дата создания записи                      |
| updated_at                | timestamp    | Да           | Дата последнего обновления                |
| is_archived               | boolean      | Да           | Признак архивной записи (мягкое удаление) |

#### Статусы оборудования

| Значение       | Описание                        |
| -------------- | ------------------------------- |
| in_use         | В эксплуатации                  |
| in_storage     | На складе                       |
| in_repair      | В ремонте                       |
| written_off    | Списано                         |
| reserved       | Зарезервировано                 |

### 4.3. Модуль справочников

| ID     | Требование                                                    |
| ------ | ------------------------------------------------------------- |
| REF-1  | CRUD для категорий оборудования (Компьютеры, Мебель и т.д.)   |
| REF-2  | CRUD для подразделений / помещений                            |
| REF-3  | Справочник статусов (предустановленный, нередактируемый)       |

#### Категория оборудования

| Поле        | Тип    | Описание              |
| ----------- | ------ | --------------------- |
| id          | UUID   | Уникальный идентификатор |
| name        | string | Название категории    |
| description | text   | Описание              |

#### Подразделение

| Поле        | Тип    | Описание              |
| ----------- | ------ | --------------------- |
| id          | UUID   | Уникальный идентификатор |
| name        | string | Название              |
| location    | string | Адрес / расположение  |

### 4.4. Модуль инвентаризации

| ID    | Требование                                                                     |
| ----- | ------------------------------------------------------------------------------ |
| INV-1 | Создание сессии инвентаризации (дата, подразделение, ответственный)             |
| INV-2 | Поэлементная проверка — отметка наличия/отсутствия каждой единицы оборудования |
| INV-3 | Фиксация расхождений (ожидаемое vs фактическое)                               |
| INV-4 | Завершение сессии с формированием акта инвентаризации                           |
| INV-5 | Просмотр истории инвентаризаций                                                |
| INV-6 | Экспорт акта инвентаризации в CSV                                              |

#### Сессия инвентаризации

| Поле            | Тип       | Описание                                       |
| --------------- | --------- | ---------------------------------------------- |
| id              | UUID      | Уникальный идентификатор                       |
| department_id   | UUID (FK) | Инвентаризируемое подразделение                |
| started_at      | timestamp | Начало инвентаризации                          |
| finished_at     | timestamp | Завершение (null — если в процессе)            |
| status          | enum      | in_progress / completed                        |
| created_by      | UUID (FK) | Кто создал сессию                              |

#### Элемент инвентаризации

| Поле              | Тип       | Описание                                     |
| ----------------- | --------- | -------------------------------------------- |
| id                | UUID      | Уникальный идентификатор                     |
| session_id        | UUID (FK) | Сессия инвентаризации                        |
| equipment_id      | UUID (FK) | Единица оборудования                         |
| expected_status   | enum      | Ожидаемый статус                             |
| actual_status     | enum      | Фактический статус (found / not_found / damaged) |
| comment           | text      | Комментарий                                  |
| checked_at        | timestamp | Время проверки                               |

### 4.5. Модуль перемещений

| ID    | Требование                                                                |
| ----- | ------------------------------------------------------------------------- |
| MV-1  | Регистрация перемещения оборудования между подразделениями                |
| MV-2  | История перемещений по каждой единице оборудования                        |
| MV-3  | Автоматическое обновление текущего подразделения в карточке оборудования  |

#### Запись о перемещении

| Поле              | Тип       | Описание                   |
| ----------------- | --------- | -------------------------- |
| id                | UUID      | Уникальный идентификатор   |
| equipment_id      | UUID (FK) | Единица оборудования       |
| from_department   | UUID (FK) | Откуда                     |
| to_department     | UUID (FK) | Куда                       |
| moved_by          | UUID (FK) | Кто переместил             |
| moved_at          | timestamp | Дата перемещения           |
| reason            | text      | Причина перемещения        |

### 4.6. Модуль отчётности

| ID    | Требование                                                          |
| ----- | ------------------------------------------------------------------- |
| RPT-1 | Сводный отчёт по оборудованию (количество по категориям и статусам) |
| RPT-2 | Отчёт по подразделению (список оборудования с суммарной стоимостью) |
| RPT-3 | Отчёт по результатам инвентаризации (расхождения)                   |
| RPT-4 | Отчёт по движению оборудования за период                            |
| RPT-5 | Дашборд на главной странице с ключевыми показателями                |

#### Дашборд — виджеты

- Общее количество единиц оборудования.
- Распределение по статусам (круговая диаграмма).
- Распределение по категориям (столбчатая диаграмма).
- Последние перемещения (таблица, 10 записей).
- Оборудование с истекающей гарантией (ближайшие 30 дней).

### 4.7. Модуль управления пользователями (Администрирование)

| ID     | Требование                                                   |
| ------ | ------------------------------------------------------------ |
| USR-1  | Создание учётной записи пользователя                         |
| USR-2  | Редактирование профиля пользователя                          |
| USR-3  | Блокировка / разблокировка пользователя                      |
| USR-4  | Назначение роли пользователю                                 |
| USR-5  | Просмотр списка пользователей                                |

#### Пользователь

| Поле         | Тип       | Описание                  |
| ------------ | --------- | ------------------------- |
| id           | UUID      | Уникальный идентификатор  |
| username     | string    | Логин (уникальный)        |
| password_hash| string    | Хэш пароля (bcrypt)       |
| full_name    | string    | ФИО                       |
| email        | string    | Электронная почта          |
| role         | enum      | admin / inventory / viewer |
| is_active    | boolean   | Активна ли учётная запись  |
| created_at   | timestamp | Дата создания              |

### 4.8. Журнал действий (Аудит)

| ID     | Требование                                                     |
| ------ | -------------------------------------------------------------- |
| LOG-1  | Автоматическая запись действий: создание, обновление, удаление  |
| LOG-2  | Просмотр журнала с фильтрацией по пользователю, дате, действию |
| LOG-3  | Журнал доступен только администратору                          |

#### Запись журнала

| Поле        | Тип       | Описание                                      |
| ----------- | --------- | --------------------------------------------- |
| id          | UUID      | Уникальный идентификатор                      |
| user_id     | UUID (FK) | Кто совершил действие                         |
| action      | string    | Тип действия (create / update / delete / login) |
| entity_type | string    | Тип сущности (equipment, department и т.д.)   |
| entity_id   | UUID      | ID сущности                                   |
| details     | jsonb     | Детали изменения (старые/новые значения)      |
| created_at  | timestamp | Время действия                                |

---

## 5. API-эндпоинты (Backend — Go)

### 5.1. Аутентификация

| Метод | Путь                    | Описание              | Доступ     |
| ----- | ----------------------- | --------------------- | ---------- |
| POST  | /api/v1/auth/login      | Авторизация           | Все        |
| POST  | /api/v1/auth/refresh    | Обновление токена     | Все        |
| POST  | /api/v1/auth/logout     | Выход                 | Авторизованные |
| PUT   | /api/v1/auth/password   | Смена пароля          | Авторизованные |

### 5.2. Пользователи

| Метод  | Путь                     | Описание                  | Доступ |
| ------ | ------------------------ | ------------------------- | ------ |
| GET    | /api/v1/users            | Список пользователей      | admin  |
| POST   | /api/v1/users            | Создание пользователя     | admin  |
| GET    | /api/v1/users/:id        | Профиль пользователя      | admin  |
| PUT    | /api/v1/users/:id        | Обновление пользователя   | admin  |
| PATCH  | /api/v1/users/:id/status | Блокировка / разблокировка | admin |

### 5.3. Оборудование

| Метод  | Путь                              | Описание                    | Доступ            |
| ------ | --------------------------------- | --------------------------- | ----------------- |
| GET    | /api/v1/equipment                 | Список оборудования         | Все авторизованные |
| POST   | /api/v1/equipment                 | Создание карточки           | admin, inventory  |
| GET    | /api/v1/equipment/:id             | Карточка оборудования       | Все авторизованные |
| PUT    | /api/v1/equipment/:id             | Обновление карточки         | admin, inventory  |
| DELETE | /api/v1/equipment/:id             | Архивация                   | admin             |
| POST   | /api/v1/equipment/:id/photos      | Загрузка фото               | admin, inventory  |
| DELETE | /api/v1/equipment/:id/photos/:pid | Удаление фото               | admin, inventory  |
| GET    | /api/v1/equipment/export/csv      | Экспорт в CSV               | Все авторизованные |

### 5.4. Категории

| Метод  | Путь                      | Описание             | Доступ             |
| ------ | ------------------------- | -------------------- | ------------------ |
| GET    | /api/v1/categories        | Список категорий     | Все авторизованные |
| POST   | /api/v1/categories        | Создание категории   | admin              |
| PUT    | /api/v1/categories/:id    | Обновление категории | admin              |
| DELETE | /api/v1/categories/:id    | Удаление категории   | admin              |

### 5.5. Подразделения

| Метод  | Путь                       | Описание                | Доступ             |
| ------ | -------------------------- | ----------------------- | ------------------ |
| GET    | /api/v1/departments        | Список подразделений    | Все авторизованные |
| POST   | /api/v1/departments        | Создание подразделения  | admin              |
| PUT    | /api/v1/departments/:id    | Обновление              | admin              |
| DELETE | /api/v1/departments/:id    | Удаление                | admin              |

### 5.6. Инвентаризация

| Метод  | Путь                                     | Описание                   | Доступ           |
| ------ | ---------------------------------------- | -------------------------- | ---------------- |
| GET    | /api/v1/inventories                      | Список сессий              | admin, inventory |
| POST   | /api/v1/inventories                      | Создание сессии            | admin, inventory |
| GET    | /api/v1/inventories/:id                  | Детали сессии              | admin, inventory |
| POST   | /api/v1/inventories/:id/items            | Отметка единицы            | admin, inventory |
| PUT    | /api/v1/inventories/:id/items/:itemId    | Обновление отметки         | admin, inventory |
| POST   | /api/v1/inventories/:id/complete         | Завершение сессии          | admin, inventory |
| GET    | /api/v1/inventories/:id/export/csv       | Экспорт акта в CSV         | admin, inventory |

### 5.7. Перемещения

| Метод | Путь                                    | Описание                           | Доступ           |
| ----- | --------------------------------------- | ---------------------------------- | ---------------- |
| POST  | /api/v1/movements                       | Регистрация перемещения            | admin, inventory |
| GET   | /api/v1/equipment/:id/movements         | История перемещений оборудования   | Все авторизованные |

### 5.8. Отчёты

| Метод | Путь                            | Описание                       | Доступ             |
| ----- | ------------------------------- | ------------------------------ | ------------------ |
| GET   | /api/v1/reports/summary         | Сводный отчёт                  | Все авторизованные |
| GET   | /api/v1/reports/by-department   | Отчёт по подразделениям        | Все авторизованные |
| GET   | /api/v1/reports/movements       | Отчёт по перемещениям          | Все авторизованные |
| GET   | /api/v1/reports/dashboard       | Данные для дашборда            | Все авторизованные |

### 5.9. Журнал действий

| Метод | Путь                  | Описание        | Доступ |
| ----- | --------------------- | --------------- | ------ |
| GET   | /api/v1/audit-logs    | Журнал действий | admin  |

---

## 6. Схема базы данных (PostgreSQL)

```sql
-- Расширения
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Пользователи
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(150) NOT NULL,
    email VARCHAR(150) UNIQUE NOT NULL,
    role VARCHAR(20) NOT NULL CHECK (role IN ('admin', 'inventory', 'viewer')),
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Категории оборудования
CREATE TABLE categories (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(100) UNIQUE NOT NULL,
    description TEXT,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Подразделения
CREATE TABLE departments (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(150) UNIQUE NOT NULL,
    location VARCHAR(255),
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Оборудование
CREATE TABLE equipment (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    inventory_number VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    category_id UUID NOT NULL REFERENCES categories(id),
    serial_number VARCHAR(100),
    model VARCHAR(150),
    manufacturer VARCHAR(150),
    purchase_date DATE,
    purchase_price NUMERIC(12, 2),
    warranty_expiry DATE,
    status VARCHAR(20) NOT NULL DEFAULT 'in_storage'
        CHECK (status IN ('in_use', 'in_storage', 'in_repair', 'written_off', 'reserved')),
    department_id UUID NOT NULL REFERENCES departments(id),
    responsible_person_id UUID REFERENCES users(id),
    notes TEXT,
    is_archived BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Фотографии оборудования
CREATE TABLE equipment_photos (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    equipment_id UUID NOT NULL REFERENCES equipment(id) ON DELETE CASCADE,
    file_path VARCHAR(500) NOT NULL,
    uploaded_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Перемещения
CREATE TABLE movements (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    equipment_id UUID NOT NULL REFERENCES equipment(id),
    from_department_id UUID NOT NULL REFERENCES departments(id),
    to_department_id UUID NOT NULL REFERENCES departments(id),
    moved_by UUID NOT NULL REFERENCES users(id),
    moved_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    reason TEXT
);

-- Сессии инвентаризации
CREATE TABLE inventory_sessions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    department_id UUID NOT NULL REFERENCES departments(id),
    status VARCHAR(20) NOT NULL DEFAULT 'in_progress'
        CHECK (status IN ('in_progress', 'completed')),
    created_by UUID NOT NULL REFERENCES users(id),
    started_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    finished_at TIMESTAMP WITH TIME ZONE
);

-- Элементы инвентаризации
CREATE TABLE inventory_items (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    session_id UUID NOT NULL REFERENCES inventory_sessions(id) ON DELETE CASCADE,
    equipment_id UUID NOT NULL REFERENCES equipment(id),
    expected_status VARCHAR(20) NOT NULL,
    actual_status VARCHAR(20) NOT NULL CHECK (actual_status IN ('found', 'not_found', 'damaged')),
    comment TEXT,
    checked_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Журнал действий
CREATE TABLE audit_logs (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id),
    action VARCHAR(50) NOT NULL,
    entity_type VARCHAR(50) NOT NULL,
    entity_id UUID,
    details JSONB,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Refresh-токены
CREATE TABLE refresh_tokens (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token_hash VARCHAR(255) UNIQUE NOT NULL,
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_equipment_category ON equipment(category_id);
CREATE INDEX idx_equipment_department ON equipment(department_id);
CREATE INDEX idx_equipment_status ON equipment(status);
CREATE INDEX idx_equipment_inventory_number ON equipment(inventory_number);
CREATE INDEX idx_equipment_is_archived ON equipment(is_archived);
CREATE INDEX idx_movements_equipment ON movements(equipment_id);
CREATE INDEX idx_movements_moved_at ON movements(moved_at);
CREATE INDEX idx_inventory_items_session ON inventory_items(session_id);
CREATE INDEX idx_audit_logs_user ON audit_logs(user_id);
CREATE INDEX idx_audit_logs_entity ON audit_logs(entity_type, entity_id);
CREATE INDEX idx_audit_logs_created ON audit_logs(created_at);
```

---

## 7. Архитектура системы

### 7.1. Общая схема

```
┌─────────────────┐     HTTP/JSON     ┌─────────────────┐     SQL      ┌──────────────┐
│                 │  ◄──────────────►  │                 │ ◄──────────► │              │
│   Frontend      │                   │   Backend API   │              │  PostgreSQL   │
│   (Next.js)     │                   │   (Go)          │              │              │
│   :3000         │                   │   :8080         │              │  :5432       │
│                 │                   │                 │              │              │
└─────────────────┘                   └─────────────────┘              └──────────────┘
        │                                     │
        │                                     │
   Podman Container                    Podman Container               Podman Container
```

### 7.2. Структура Backend (Go)

```
backend/
├── cmd/
│   └── server/
│       └── main.go              # Точка входа
├── internal/
│   ├── config/
│   │   └── config.go            # Конфигурация (env-переменные)
│   ├── handler/                  # HTTP-обработчики
│   │   ├── auth.go
│   │   ├── equipment.go
│   │   ├── category.go
│   │   ├── department.go
│   │   ├── inventory.go
│   │   ├── movement.go
│   │   ├── report.go
│   │   ├── user.go
│   │   └── audit.go
│   ├── middleware/               # JWT, RBAC, логирование, CORS
│   │   ├── auth.go
│   │   ├── rbac.go
│   │   ├── logger.go
│   │   └── cors.go
│   ├── model/                    # Структуры данных
│   │   ├── user.go
│   │   ├── equipment.go
│   │   ├── category.go
│   │   ├── department.go
│   │   ├── inventory.go
│   │   ├── movement.go
│   │   └── audit.go
│   ├── repository/               # Слой работы с БД
│   │   ├── user.go
│   │   ├── equipment.go
│   │   ├── category.go
│   │   ├── department.go
│   │   ├── inventory.go
│   │   ├── movement.go
│   │   └── audit.go
│   ├── service/                  # Бизнес-логика
│   │   ├── auth.go
│   │   ├── equipment.go
│   │   ├── category.go
│   │   ├── department.go
│   │   ├── inventory.go
│   │   ├── movement.go
│   │   ├── report.go
│   │   └── audit.go
│   └── router/
│       └── router.go            # Маршрутизация
├── migrations/                   # SQL-миграции
│   ├── 001_init.up.sql
│   └── 001_init.down.sql
├── uploads/                      # Загруженные файлы (volume)
├── go.mod
├── go.sum
├── Containerfile
└── .env.example
```

### 7.3. Структура Frontend (Next.js)

```
frontend/
├── src/
│   ├── app/                      # App Router (Next.js 14+)
│   │   ├── layout.tsx
│   │   ├── page.tsx              # Главная / Дашборд
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── equipment/
│   │   │   ├── page.tsx          # Список оборудования
│   │   │   ├── [id]/
│   │   │   │   └── page.tsx      # Карточка оборудования
│   │   │   └── new/
│   │   │       └── page.tsx      # Создание
│   │   ├── categories/
│   │   │   └── page.tsx
│   │   ├── departments/
│   │   │   └── page.tsx
│   │   ├── inventory/
│   │   │   ├── page.tsx          # Список сессий
│   │   │   └── [id]/
│   │   │       └── page.tsx      # Процесс инвентаризации
│   │   ├── reports/
│   │   │   └── page.tsx
│   │   ├── users/
│   │   │   └── page.tsx
│   │   └── audit/
│   │       └── page.tsx
│   ├── components/
│   │   ├── ui/                   # Базовые UI-компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Table.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Pagination.tsx
│   │   │   ├── Select.tsx
│   │   │   └── Badge.tsx
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── MainLayout.tsx
│   │   ├── equipment/
│   │   │   ├── EquipmentTable.tsx
│   │   │   ├── EquipmentForm.tsx
│   │   │   ├── EquipmentCard.tsx
│   │   │   └── EquipmentFilters.tsx
│   │   ├── inventory/
│   │   │   ├── InventoryList.tsx
│   │   │   └── InventoryCheckForm.tsx
│   │   ├── dashboard/
│   │   │   ├── StatsCard.tsx
│   │   │   ├── StatusChart.tsx
│   │   │   └── RecentMovements.tsx
│   │   └── auth/
│   │       └── LoginForm.tsx
│   ├── lib/
│   │   ├── api.ts                # HTTP-клиент (fetch-обёртка)
│   │   ├── auth.ts               # Работа с токенами
│   │   └── utils.ts              # Утилиты
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useEquipment.ts
│   │   └── usePagination.ts
│   └── types/
│       └── index.ts              # TypeScript-типы
├── public/
├── tailwind.config.ts
├── tsconfig.json
├── next.config.js
├── package.json
├── Containerfile
└── .env.example
```

---

## 8. Контейнеризация (Podman)

### 8.1. podman-compose.yml

```yaml
version: "3.8"

services:
  db:
    image: docker.io/library/postgres:16-alpine
    container_name: inventory-db
    environment:
      POSTGRES_DB: inventory
      POSTGRES_USER: inventory_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventory_user -d inventory"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      containerfile: Containerfile
    container_name: inventory-backend
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: inventory
      DB_USER: inventory_user
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      containerfile: Containerfile
    container_name: inventory-frontend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8080/api/v1
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pg_data:
  uploads:
```

### 8.2. Containerfile (Backend)

```dockerfile
FROM docker.io/library/golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o server ./cmd/server

FROM docker.io/library/alpine:3.19
RUN apk --no-cache add ca-certificates
WORKDIR /app
COPY --from=builder /app/server .
COPY --from=builder /app/migrations ./migrations
EXPOSE 8080
CMD ["./server"]
```

### 8.3. Containerfile (Frontend)

```dockerfile
FROM docker.io/library/node:20-alpine AS builder
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM docker.io/library/node:20-alpine
WORKDIR /app
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public
EXPOSE 3000
CMD ["node", "server.js"]
```

---

## 9. Нефункциональные требования

### 9.1. Производительность

- Время отклика API — не более 500 мс для стандартных операций.
- Пагинация — не более 50 записей на страницу по умолчанию.
- Поддержка одновременной работы не менее 20 пользователей.

### 9.2. Безопасность

- Пароли хранятся в виде bcrypt-хэшей (cost = 12).
- JWT-токены передаются в заголовке `Authorization: Bearer <token>`.
- Refresh-токены хранятся в HttpOnly cookie.
- Защита от CSRF, XSS, SQL-инъекций.
- CORS настроен на разрешённые origins.
- Валидация всех входных данных на стороне backend.
- Rate limiting на эндпоинтах аутентификации.

### 9.3. Надёжность

- Миграции БД выполняются при старте backend-сервиса.
- Graceful shutdown backend-сервиса.
- Данные PostgreSQL хранятся в Podman volume (персистентность).

### 9.4. Удобство использования

- Адаптивный интерфейс (desktop: 1280px+, tablet: 768px+).
- Уведомления об успешных/ошибочных операциях (toast-уведомления).
- Подтверждение перед удалением / архивацией.
- Хлебные крошки для навигации.

---

## 10. Рекомендуемые библиотеки

### Backend (Go)

| Библиотека              | Назначение                     |
| ----------------------- | ------------------------------ |
| chi или gin             | HTTP-роутер                    |
| pgx                     | Драйвер PostgreSQL             |
| golang-jwt/jwt          | Работа с JWT                   |
| golang-migrate/migrate  | Миграции БД                    |
| go-playground/validator | Валидация входных данных        |
| rs/cors                 | CORS-middleware                 |
| slog (stdlib)           | Логирование                    |
| swaggo/swag             | Генерация Swagger-документации |

### Frontend (Next.js)

| Библиотека         | Назначение                    |
| ------------------ | ----------------------------- |
| react-hook-form    | Управление формами            |
| zod                | Валидация схем данных         |
| recharts           | Графики и диаграммы           |
| react-hot-toast    | Toast-уведомления             |
| lucide-react       | Иконки                        |
| clsx / tailwind-merge | Утилиты для классов CSS    |

---

## 11. Этапы разработки

| Этап | Описание                                               | Приоритет |
| ---- | ------------------------------------------------------ | --------- |
| 1    | Настройка инфраструктуры (Podman, БД, миграции, CI)    | Высокий   |
| 2    | Аутентификация и управление пользователями              | Высокий   |
| 3    | Справочники (категории, подразделения)                  | Высокий   |
| 4    | CRUD оборудования + загрузка фото                       | Высокий   |
| 5    | Поиск, фильтрация, пагинация, экспорт                  | Средний   |
| 6    | Модуль перемещений                                      | Средний   |
| 7    | Модуль инвентаризации                                   | Средний   |
| 8    | Отчётность и дашборд                                    | Средний   |
| 9    | Журнал аудита                                           | Низкий    |
| 10   | Тестирование, отладка, документация                     | Высокий   |

---

## 12. Формат ответов API

### Успешный ответ

```json
{
  "data": { ... },
  "meta": {
    "page": 1,
    "per_page": 20,
    "total": 150,
    "total_pages": 8
  }
}
```

### Ответ с ошибкой

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Поле 'name' обязательно для заполнения",
    "details": [
      {
        "field": "name",
        "message": "обязательное поле"
      }
    ]
  }
}
```

### HTTP-коды ответов

| Код | Описание                       |
| --- | ------------------------------ |
| 200 | Успешная операция              |
| 201 | Ресурс создан                  |
| 204 | Успех без тела ответа          |
| 400 | Ошибка валидации               |
| 401 | Не авторизован                 |
| 403 | Доступ запрещён                |
| 404 | Ресурс не найден               |
| 409 | Конфликт (дублирование)        |
| 500 | Внутренняя ошибка сервера      |
